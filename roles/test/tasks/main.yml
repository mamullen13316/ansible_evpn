---
  - name: What is the python path?
    command: python -c "import sys; print sys.path"

  - name: Can I import openpyxl?
    command: python -c "import openpyxl; print openpyxl.__version__"

  - name: Import host-specific facts from a CSV file
    test_xls_to_facts:
      src: "evpn_config_template.xlsx" 
